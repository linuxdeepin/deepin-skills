# org.gnome.keyring.Daemon 接口参考

该接口提供 GNOME 密钥环守护进程控制能力。来源于上游 GNOME 项目。详见源码获取完整接口定义。

## 接口信息

| 字段 | 值 |
|------|------|
| Service | `org.gnome.keyring` |
| Object path | `/org/gnome/keyring/daemon` |
| Interface | `org.gnome.keyring.Daemon` |
| Bus | Session |

> **待核验声明**：本文档接口信息基于源码静态分析，未经运行时 D-Bus 内省验证，标记为待核验。

> 详见源码获取完整的密钥环守护进程接口定义。
