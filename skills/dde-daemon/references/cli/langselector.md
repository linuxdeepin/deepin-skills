# langselector 命令参考

语言选择守护进程，负责管理系统的语言和区域设置。

## 基本信息

| 字段 | 值 |
|------|------|
| 所属包名 | `dde-daemon` |
| 安装路径 | `/usr/lib/deepin-daemon/langselector` |
| DDE 角色 | 系统语言和区域设置管理服务 |

## 用途

语言选择守护进程，负责管理系统的语言和区域设置。它通过 DBus 提供语言列表查询和语言切换接口，配合控制中心的语言设置模块工作。该进程由 DBus 激活，一般不需要用户直接运行。
