**用途**：DDE 文件搜索工具，是 dde-daemon 提供的文件全文搜索引擎后端。它负责建立文件索引并提供搜索服务，通过 DBus 接口供文件管理器等应用调用。该进程由 DBus 激活，一般不需要用户直接运行。

- **所属包名**：`dde-daemon`
- **安装路径**：`/usr/lib/deepin-daemon/search`
- **DDE 角色**：文件搜索服务的后台引擎
