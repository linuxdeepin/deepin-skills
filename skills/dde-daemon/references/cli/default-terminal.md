# default-terminal 命令参考

默认终端设置工具，用于查询和设置系统默认终端模拟器。

## 基本信息

| 字段 | 值 |
|------|------|
| 所属包名 | `dde-daemon` |
| 安装路径 | `/usr/lib/deepin-daemon/default-terminal` |
| DDE 角色 | 默认终端应用的配置工具 |

## 用途

默认终端设置工具，用于查询和设置系统默认终端模拟器。它读取和修改系统配置中终端相关的默认应用设置，配合控制中心的默认应用管理使用，不提供 flag 参数。
