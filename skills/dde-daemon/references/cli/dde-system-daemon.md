**用途**：DDE 系统守护进程，是 DDE 桌面环境中运行在系统级别的核心后台服务。它负责管理网络、蓝牙、电源、声音、账户等系统级模块，通过 DBus 提供系统管理接口。该守护进程在系统启动时自动运行，是 DDE 桌面环境正常运作的基础服务。

- **所属包名**：`dde-daemon`
- **安装路径**：`/usr/bin/deepin-system-daemon`
- **DDE 角色**：系统级核心后台服务，管理硬件和系统模块
