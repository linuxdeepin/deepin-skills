---
name: dtkgui
description: DTK GUI 组件库（暂无对外暴露的独立 D-Bus 服务接口）
Categories:
  - Develop
---

# dtkgui

dtkgui 是 DTK GUI 组件库。此前文档中记录的 `org.deepin.dtk.DFileDragServer` 接口为进程内对象，非独立 D-Bus 服务（实际服务名为 `com.deepin.dtk.FileDrag`），已移除。
