# com.deepin.dtk.FileDrag

## 说明

此前文档中记录的 `org.deepin.dtk.DFileDragServer` 接口为进程内对象，非独立 D-Bus 服务（实际服务名为 `com.deepin.dtk.FileDrag`），已移除。

如需进一步确认 FileDrag 的 D-Bus 能力，请以 dtkgui 源码中的服务端注册代码和 introspection XML 为准进行运行时内省验证。
